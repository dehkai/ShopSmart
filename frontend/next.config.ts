import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // BACKEND_URL is server-side only — never exposed to browser
  // Set in .env.local: BACKEND_URL=http://localhost:8001
}

export default nextConfig
